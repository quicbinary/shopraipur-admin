// next.config.js

module.exports = {
    images: {
      domains: ['picsum.photos', 'via.placeholder.com'], // Add the external image domain
    },
    experimental: {
        appDir: true, // Ensure that the app directory is enabled
      },
  };
  