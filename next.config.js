// next.config.js

module.exports = {
    images: {
      domains: ['picsum.photos', 'via.placeholder.com', 'img.freepik.com'], // Add the external image domain
    },
    experimental: {
        appDir: true, // Ensure that the app directory is enabled
      },
  };
  